export function About() {
	return (
		<main className="mx-auto grid max-w-prose gap-4">
			<h1 className="text-3xl font-bold text-brand-700">About</h1>
			<p className="text-gray-600">A second route, so the nav has something to switch to.</p>
		</main>
	);
}
